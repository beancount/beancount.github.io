# Beancount Project Site

This repository contains the source for <https://beancount.github.io/>.

Build the site locally with:

```shell
make build
```

Serve it locally with:

```shell
make serve
```
